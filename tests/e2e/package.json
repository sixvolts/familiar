{
  "name": "familiar-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "End-to-end tests for Familiar (gateway + workspace). See MAKE_TESTS.md.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "screenshot": "playwright test --config screenshot.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^20.10.0",
    "@types/pg": "^8.11.0",
    "pg": "^8.13.0",
    "typescript": "^5.3.0"
  }
}
