The research workers for "{{TOPIC}}" have finished. Their findings are
on the evidence page: book_slug="{{EVIDENCE_BOOK}}",
page_slug="{{EVIDENCE_PAGE}}".

**This is a comprehensive deep-research deliverable — the deliberately
high-effort task that warrants FULL, exhaustive detail.** Any general
guidance you carry to "be brief", "keep it short", "match length to
complexity", or avoid length does NOT apply here: deep research IS the
complex case those rules point AT, so thoroughness is the whole point.
Write the complete write-up the evidence supports and err long — a
reader chose deep research precisely because they want depth, not a
skim. "No filler" means no padding or restatement, never "be short":
cut fluff, keep substance.

Do exactly this, in order:

1. read_page that evidence page. It is your ONLY source — do not run
   new web searches.
2. update_page the note at book_slug="{{NOTE_BOOK}}",
   page_slug="{{NOTE_PAGE}}" with the full research write-up in markdown:
   Summary / Key findings / Details / Sources. **Aim for 900–1,600+
   words of substance (excluding Sources), and go longer when the
   evidence supports it — a deep note under ~900 words has almost
   certainly left gathered evidence on the table.** Develop every angle
   the workers covered to its full depth. Write a deliverable someone
   will read cold — a coherent, explanatory briefing with a
   through-line, not a log of findings. Connect facts into a narrative;
   state each once in its most specific section (Details adds depth
   beyond Key findings, never restates it). Put an inline [Title](URL)
   citation at every claim, drawn only from the evidence; list only
   sources you used, each annotated with what it supported. **End on
   Sources — do NOT add an "Open questions", "Further research", or
   "Unanswered" section.** Do NOT link the evidence page (it is
   temporary).
3. Run the memory pass: 10–20 save_fact calls, scope "user", tags
   ["research", "{{TOPIC_SLUG}}"], one atomic self-contained sentence
   each with named entities and exact numbers/versions; put a source URL
   in the content when it matters; "as of <month year>" on
   time-sensitive claims. Facts only, no speculation.
4. Reply with a ≤200-word chat summary: the 3–5 most useful or
   surprising takeaways (not a rehash of the note's own Summary). Do not
   state where the note lives or paste a link — the interface adds a link
   to the note automatically. Nothing else.

Do not spawn more workers — synthesize from the evidence you have.
